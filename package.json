{
    "name": "my-project",
    "version": "1.0.0",
    "scripts": {
        "dev": "rollup -c -w",
        "start": "sirv public --port 5000",
        "build": "rollup -c && npm run build:css",
        "build:css": "postcss public/build/bundle.css -o public/build/bundle.css"
    },
    "devDependencies": {
        "@rollup/plugin-commonjs": "^21.0.1",
        "@rollup/plugin-node-resolve": "^13.0.0",
        "rollup": "^2.56.3",
        "rollup-plugin-livereload": "^2.0.0",
        "sirv-cli": "^1.0.0",
        "svelte": "^3.0.0"
    },
    "dependencies": {
        "plausible-tracker": "^0.3.8"
    }
}
